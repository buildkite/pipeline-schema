# Test pipeline with secrets in object/map format at pipeline level
secrets:
  GLOBAL_API_TOKEN: API_ACCESS_TOKEN
  GLOBAL_DB_URL: DATABASE_URL

steps:
  - command: echo "Using global secrets in object format"
    label: ":rocket: Step with global object format secrets"

  - command: echo "Using both global and step-specific secrets"
    label: ":lock: Step with additional mapped secrets"
    secrets:
      CUSTOM_DEPLOYMENT_KEY: DEPLOYMENT_KEY
