{
  "name": "buildkite-pipeline-schema",
  "description": "A JSON schema for Buildkite’s pipeline file format",
  "keywords": [
    "buildkite",
    "pipeline",
    "schema"
  ],
  "version": "1.0.0",
  "homepage": "https://github.com/buildkite/pipeline-schema",
  "bugs": "https://github.com/buildkite/pipeline-schema/issues",
  "files": [
    "LICENSE.md",
    "README.md",
    "schema.json"
  ],
  "main": "schema.json",
  "license": "MIT",
  "scripts": {
    "test": "vitest",
    "preversion": "vitest --run && prettier --check ."
  },
  "devDependencies": {
    "ajv": "^6.12.3",
    "js-yaml": "^3.13.1",
    "prettier": "3.2.5",
    "vitest": "^1.2.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/buildkite/buildkite-pipeline-schema.git"
  }
}
